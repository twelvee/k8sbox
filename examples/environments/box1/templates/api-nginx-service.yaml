apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.Environment }}-{{ .Release.Name }}-box1"
  labels:
    helm/chart: "{{ .Chart.Name }}-{{ .Chart.Version }}-box1"
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: "{{ .Values.Environment }}-{{ .Release.Name }}-box1"
    app.kubernetes.io/instance: "{{ .Values.Environment }}-{{ .Release.Name }}-box1"